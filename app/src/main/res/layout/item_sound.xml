<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:backgroundTint="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Circular Icon Container -->
        <androidx.cardview.widget.CardView
            android:layout_width="56dp"
            android:layout_height="56dp"
            app:cardCornerRadius="28dp"
            app:cardElevation="0dp"
            android:backgroundTint="#E0F5FE"> <!-- Light Blue Background -->

            <ImageView
                android:id="@+id/image_sound_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Sound Icon"
                android:padding="1dp"
                android:scaleType="centerInside"
         /> <!-- Tinting the icon with the primary color -->
            />
        </androidx.cardview.widget.CardView>

        <!-- Sound Name (Dynamically set by Adapter ) -->
        <TextView
            android:id="@+id/text_sound_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:text="Sound Name Here"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <!-- Play Button -->
        <ImageView
            android:id="@+id/image_play_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="Play Sound"
            android:padding="4dp"
            android:src="@drawable/ic_volume_up"
             />

    </LinearLayout>
</androidx.cardview.widget.CardView>
